# 《创新小学作业管理系统》开发说明书

> 版本：v1.0  
> 日期：2026-04-16  
> 项目类型：响应式Web应用（支持手机/平板/电脑）

---

## 一、项目概述

### 1.1 项目背景
为小学教育场景设计的一套在线作业布置与批改系统，旨在减轻教师批改负担、提升学生学习体验、实现家校互联。

### 1.2 目标用户
| 角色 | 说明 |
|------|------|
| 学生 | 小学生（1-6年级），通过学号登录答题 |
| 教师 | 负责班级管理、题库维护、出题发布 |
| 管理员 | 系统运维、教师管理、全局配置 |

### 1.3 设计风格
- **主题**：色彩丰富、活泼可爱，适合小学低龄儿童
- **配色方案**：主色调为明亮的橙色/蓝色，辅以绿色（成功）、红色（警示）
- **图标风格**：圆角、卡通化SVG矢量图标，避免使用emoji
- **字体**：思源黑体/圆体，字号偏大便于阅读

### 1.4 技术选型建议
| 层级 | 推荐方案 | 备选方案 |
|------|----------|----------|
| 前端框架 | Vue 3 + Vant UI | React + Ant Design Mobile |
| 后端框架 | Node.js + Express | Python + FastAPI |
| 数据库 | SQLite（轻量部署） | MySQL（大规模部署） |
| AI批改 | DeepSeek V3 API | 百度千帆、阿里百炼 |
| 文件存储 | 本地存储 | 阿里云OSS/腾讯云COS |

---

## 二、功能模块详解

### 2.1 用户认证与权限系统

#### 2.1.1 登录方式
| 角色 | 登录凭证 | 密码规则 |
|------|----------|----------|
| 学生 | 学号（如：202601001） | 初始密码：123456，首次登录强制修改 |
| 教师 | 工号（如：T2026001） | 同上 |
| 管理员 | admin | 独立设置，需高强度密码 |

#### 2.1.2 权限矩阵
| 功能模块 | 学生 | 教师 | 管理员 |
|----------|------|------|--------|
| 答题 | ✅ | ❌ | ❌ |
| 查看成绩 | ✅（仅自己） | ✅（所教班级） | ✅（全部） |
| 学生管理 | ❌ | ✅（所管班级） | ✅（全部） |
| 题库管理 | ❌ | ✅ | ✅ |
| 出题发布 | ❌ | ✅ | ✅ |
| 教师管理 | ❌ | ❌ | ✅ |
| 系统配置 | ❌ | ❌ | ✅ |

#### 2.1.3 登录安全
- 登录失败5次后锁定账户15分钟
- 支持记住登录状态（7天免登录）
- 敏感操作需二次验证（修改密码、删除数据等）

---

### 2.2 学生管理模块（教师端/管理员端）

#### 2.2.1 学生信息字段
| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| 学号 | String(12) | ✅ | 唯一标识，格式：年级+班级+序号 |
| 学生姓名 | String(20) | ✅ | 真实姓名 |
| 性别 | Enum | ❌ | 男/女 |
| 年级 | Enum | ✅ | 一年级~六年级 |
| 班级 | String(10) | ✅ | 如：1班、2班 |
| 家长姓名 | String(20) | ❌ | 监护人姓名 |
| 联系方式 | String(15) | ❌ | 家长手机号 |
| 备注 | Text | ❌ | 特殊情况说明 |
| 状态 | Enum | - | 正常/停用/毕业 |
| 创建时间 | DateTime | - | 自动记录 |
| 更新时间 | DateTime | - | 自动更新 |

#### 2.2.2 学生管理功能
1. **列表展示**
   - 支持按年级、班级筛选
   - 支持按学号、姓名搜索
   - 分页显示（每页20条）
   - 显示学生基本信息 + 答题统计

2. **单个新增**
   - 表单验证（学号唯一性检查）
   - 支持头像上传（可选）
   - 初始密码默认123456

3. **批量导入**
   - Excel模板下载（点击按钮旁显示模板链接）
   - 支持xlsx/xls格式
   - 导入前预览数据
   - 导入结果反馈（成功X条，失败X条及原因）
   - 模板字段说明页面

4. **编辑/删除**
   - 编辑时不可修改学号
   - 删除需二次确认
   - 软删除（标记为停用状态）
   - 支持批量删除

5. **密码重置**
   - 教师可重置所管班级学生密码
   - 重置后密码为123456
   - 学生下次登录需修改密码

#### 2.2.3 Excel导入模板
```
| 学号* | 学生姓名* | 性别 | 年级 | 班级 | 家长姓名 | 联系方式 | 备注 |
|-------|----------|------|------|------|----------|----------|------|
| 202601001 | 张三 | 男 | 一年级 | 1班 | 张父 | 13800138000 | |
| 202601002 | 李四 | 女 | 一年级 | 1班 | 李母 | 13900139000 | 过敏史 |
```
*注：带*号为必填项

---

### 2.3 教师管理模块（管理员端）

#### 2.3.1 教师信息字段
| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| 工号 | String(10) | ✅ | 唯一标识，如T2026001 |
| 教师姓名 | String(20) | ✅ | 真实姓名 |
| 教师性别 | Enum | ❌ | 男/女 |
| 教授科目 | Enum[] | ✅ | 可多选：语文/数学/英语/科学 |
| 联系方式 | String(15) | ❌ | 手机号 |
| 管理班级 | String[] | ❌ | 可管理的学生班级 |
| 备注 | Text | ❌ | 特殊情况 |
| 状态 | Enum | - | 正常/停用 |
| 创建时间 | DateTime | - | 自动记录 |

#### 2.3.2 教师管理功能
1. **列表展示**
   - 支持按科目、状态筛选
   - 支持按工号、姓名搜索
   - 显示管理班级数量、出题统计

2. **权限分配**
   - 分配可管理的班级
   - 分配可操作的科目
   - 可设置教师为年级组长（管理权限扩大）

3. **批量导入**
   - Excel模板下载
   - 导入时自动分配初始班级管理权限

#### 2.3.3 Excel导入模板
```
| 工号* | 教师姓名* | 教师性别 | 教授科目* | 联系方式 | 管理班级 | 备注 |
|-------|----------|----------|----------|----------|----------|------|
| T2026001 | 王老师 | 女 | 语文,数学 | 13700137000 | 一年级1班,一年级2班 | 班主任 |
| T2026002 | 李老师 | 男 | 英语 | 13700137001 | 三年级1班 | |
```

---

### 2.4 题库管理模块（教师端/管理员端）

#### 2.4.1 题库层级结构
```
题库
├── 语文
│   ├── 一年级
│   │   ├── 第一章 拼音
│   │   │   ├── 选择题
│   │   │   ├── 填空题
│   │   │   └── 主观题
│   │   ├── 第二章 汉字
│   │   └── ...
│   ├── 二年级
│   └── ...
├── 数学
├── 英语
├── 科学
└── 专项考题（综合题型）
```

#### 2.4.2 题目类型与字段

##### 选择题
| 字段名 | 类型 | 说明 |
|--------|------|------|
| 题目ID | String | 系统自动生成 |
| 题干 | Text | 支持富文本、图片 |
| 选项 | Array | A-F（最多6个选项） |
| 正确答案 | String | 如：A、ABC、ABCD |
| 解析 | Text | 答案解析说明 |
| 难度 | Enum | 简单/中等/困难 |
| 分值 | Number | 默认2分 |
| 出题次数 | Number | 统计使用次数 |
| 正确率 | Float | 统计答对比例 |
| 状态 | Enum | 启用/停用 |

##### 填空题
| 字段名 | 类型 | 说明 |
|--------|------|------|
| 题目ID | String | 系统自动生成 |
| 题干 | Text | 支持下划线标记填空位置，如"中国的首都是____" |
| 空数 | Number | 填空数量 |
| 参考答案 | Array | 每个空的参考答案，支持多个等效答案 |
| 批改方式 | Enum | AI批改/手动批改 |
| 解析 | Text | 答案解析说明 |
| 难度 | Enum | 简单/中等/困难 |
| 分值 | Number | 每空分值 |

##### 主观题
| 字段名 | 类型 | 说明 |
|--------|------|------|
| 题目ID | String | 系统自动生成 |
| 题干 | Text | 题目描述，支持图片 |
| 参考答案 | Text | 标准答案要点 |
| 评分标准 | Text | 分步骤得分说明 |
| 批改方式 | Enum | AI批改/手动批改 |
| 分值 | Number | 总分值 |
| 字数限制 | Number | 学生作答字数上限（可选） |

#### 2.4.3 题目管理功能

1. **题目录入**
   - 富文本编辑器（支持图片上传、数学公式）
   - 实时预览题目效果
   - 自动保存草稿

2. **单个导入**
   - 选择题目类型
   - 填写题干、选项、答案、解析
   - 选择所属年级、科目、章节
   - 设置难度和分值

3. **批量导入**
   - Excel模板下载（针对选择题、填空题）
   - Word模板下载（针对主观题）
   - 导入时选择目标分类

4. **题目编辑**
   - 修改题目内容
   - 调整分类位置
   - 修改难度和分值
   - 停用/启用题目

5. **题目预览与测试**
   - 预览题目显示效果
   - 模拟答题测试

6. **题目统计**
   - 按科目、章节统计题目数量
   - 题目使用频率排行
   - 题目正确率统计

#### 2.4.4 Excel导入模板（选择题）
```
| 题干* | 选项A* | 选项B* | 选项C | 选项D | 选项E | 选项F | 正确答案* | 解析 | 难度 | 分值 |
|-------|--------|--------|------|--------|--------|--------|----------|------|------|------|
| 1+1=? | 1 | 2 | 3 | 4 | | | B | 这是最简单的加法 | 简单 | 2 |
| 中国首都是？ | 北京 | 上海 | 广州 | 深圳 | | | A | 北京是中华人民共和国首都 | 简单 | 2 |
```

#### 2.4.5 Excel导入模板（填空题）
```
| 题干* | 空数* | 参考答案1* | 参考答案2 | 参考答案3 | 解析 | 批改方式 | 难度 | 分值 |
|-------|-------|----------|----------|----------|------|----------|------|------|
| 中国的首都是____ | 1 | 北京 | | | 北京是中华人民共和国首都 | AI批改 | 简单 | 2 |
| 一年有____个月，____个季节 | 2 | 12 | 4 | | 基础常识 | AI批改 | 简单 | 4 |
```

#### 2.4.6 专项考题
- 独立于常规题库
- 可包含选择、填空、主观题混合
- 用于专项训练、期末复习等场景
- 不按章节分类，按主题标签分类

---

### 2.5 出题发布模块（教师端）

#### 2.5.1 考试任务字段
| 字段名 | 类型 | 说明 |
|--------|------|------|
| 任务ID | String | 系统自动生成 |
| 标题 | String | 作业/考试名称 |
| 科目 | Enum | 语文/数学/英语/科学 |
| 年级 | Enum | 目标年级 |
| 班级 | String[] | 目标班级（可多选） |
| 出题人 | String | 教师姓名（自动） |
| 出题日期 | DateTime | 创建时间（自动） |
| 有效期开始 | DateTime | 开始答题时间 |
| 有效期结束 | DateTime | 截止时间 |
| 答题限时 | Number | 限制时长（分钟），0表示不限时 |
| 题目列表 | Array | 包含的题目ID及顺序 |
| 总分 | Number | 试卷总分 |
| 批改方式 | Enum | 全部AI/全部手动/混合模式 |
| 状态 | Enum | 草稿/已发布/进行中/已结束/已批改 |
| 参与人数 | Number | 统计 |
| 已提交人数 | Number | 统计 |

#### 2.5.2 自动出题流程
1. **选择参数**
   - 年级、科目、章节（可多选）
   - 题型及数量（如：选择题10题、填空题5题、主观题2题）
   - 难度设置（可设置难度比例）
   - 总分设置

2. **智能抽题算法**
   - 优先抽取未出过或出题次数少的题目
   - 避免短期内重复出相同题目
   - 支持设置题目权重
   - 预览抽题结果，支持换题

3. **生成预览**
   - 显示抽中的题目列表
   - 可手动替换不满意的题目
   - 调整题目顺序
   - 计算总分

#### 2.5.3 手动出题流程
1. **浏览题库**
   - 按年级→科目→章节展开
   - 支持搜索题目关键词
   - 支持筛选题型、难度

2. **选题组卷**
   - 点击添加题目到试卷
   - 拖拽调整题目顺序
   - 可设置每题分值
   - 实时显示总分

3. **试卷预览**
   - 预览完整试卷效果
   - 检查题目完整性
   - 估算答题时间

#### 2.5.4 考试任务管理
1. **任务列表**
   - 显示所有创建的任务
   - 按状态筛选（草稿/进行中/已结束）
   - 显示参与和提交情况

2. **任务操作**
   - 编辑草稿任务
   - 发布任务
   - 延长/缩短有效期
   - 取消任务（需确认）
   - 复制任务（快速创建类似任务）

3. **任务统计**
   - 参与率统计
   - 成绩分布图
   - 题目正确率排行
   - 需关注的学困生名单

---

### 2.6 答题模块（学生端）

#### 2.6.1 待答题列表
- **图标**：书本形状SVG
- **展示字段**：标题、科目、出题人、有效期、剩余时间
- **排序**：按截止时间升序（紧急优先）
- **状态标识**：
  - 未开始（灰色）
  - 进行中（绿色）
  - 即将截止（橙色，24小时内）
  - 已过期（红色）

#### 2.6.2 答题任务详情
点击待答题项目后弹出详情页：
- 标题
- 科目（带图标）
- 出题人
- 出题日期
- 有效期（开始-结束）
- 题目总数
- 答题限时
- 「开始答题」按钮

#### 2.6.3 答题界面
1. **顶部栏**
   - 答题剩余时间（倒计时显示）
   - 题目进度（如：3/20）
   - 暂停按钮（仅限不限时模式）

2. **答题区域**
   - 题目序号和题型标识
   - 题干显示（支持图片、公式）
   - 答题区域：
     - 选择题：选项按钮（选中高亮）
     - 填空题：输入框（每个空一个）
     - 主观题：多行文本框（显示字数统计）

3. **底部导航**
   - 「上一题」按钮
   - 「下一题」按钮
   - 「检查」按钮
   - 「交卷」按钮

4. **检查界面**
   - 显示所有题目序号网格
   - 已答题：绿色底色
   - 未答题：红色底色
   - 当前题：蓝色边框
   - 点击序号跳转到对应题目

5. **退出机制**
   - 点击「退出答题」弹出确认框
   - 选择「保存并退出」：答案暂存，计时暂停（限时模式）
   - 选择「直接退出」：不保存，计时继续
   - 下次进入从上次位置继续

#### 2.6.4 交卷流程
1. **确认交卷**
   - 显示未答题数量提示
   - 二次确认交卷

2. **批改结果**
   - **AI批改**：立即显示成绩、各题得分、正确答案
   - **手动批改**：显示"等待老师批改中..."，返回主界面

3. **成绩展示**
   - 总分/满分
   - 各题型得分
   - 答题用时
   - 错题列表（带解析）
   - 「返回主界面」按钮

#### 2.6.5 答题记录
1. **列表展示**
   - 标题、科目、得分、答题时间、批改状态
   - 支持按科目筛选
   - 支持按出题日期排序
   - 支持搜索

2. **详情查看**
   - 完整试卷回顾
   - 每题显示：原题、学生答案、正确答案、解析
   - 错题标红

3. **错题库**
   - 自动收集所有错题
   - 按科目、章节分类
   - 支持「重新练习」功能
   - 显示错误次数统计

---

### 2.7 批改模块（教师端）

#### 2.7.1 待批改列表
- 显示需要手动批改的考试任务
- 显示待批改人数/总人数
- 按紧急程度排序

#### 2.7.2 批改界面
1. **学生列表**
   - 左侧显示学生列表
   - 已批改学生显示成绩
   - 未批改学生高亮显示

2. **批改区域**
   - 选择题：自动批改，显示得分
   - 填空题：
     - 显示学生答案
     - 显示参考答案
     - AI批改建议分数（可参考）
     - 教师可调整分数
     - 可添加批注
   - 主观题：
     - 显示学生作答
     - 显示参考答案和评分标准
     - 分步打分
     - 添加批注

3. **批量操作**
   - 批量通过AI建议分数
   - 批量添加相同批注
   - 快速批改下一份

#### 2.7.3 成绩发布
- 全部批改完成后可发布成绩
- 发布后学生可查看成绩
- 支持单独发布某学生成绩

---

### 2.8 数据统计与分析

#### 2.8.1 教师端统计
1. **班级成绩概览**
   - 平均分、最高分、最低分
   - 成绩分布图（柱状图）
   - 及格率、优秀率

2. **题目分析**
   - 各题正确率排行
   - 易错题列表
   - 题目难度验证

3. **学生表现**
   - 成绩趋势图
   - 进步/退步学生名单
   - 学困生预警

4. **作业统计**
   - 完成率统计
   - 按时提交率
   - 批改进度

#### 2.8.2 学生端统计
1. **个人成绩趋势**
   - 折线图显示各次考试成绩
   - 科目对比

2. **知识点掌握**
   - 各章节正确率
   - 薄弱环节提示

3. **学习建议**
   - AI生成个性化学习建议

#### 2.8.3 管理员端统计
- 系统使用概况
- 题库规模统计
- 活跃用户统计
- 各班级成绩对比

---

### 2.9 系统配置模块（管理员端）

#### 2.9.1 AI批改配置
| 配置项 | 说明 |
|--------|------|
| API服务商 | DeepSeek / 百度千帆 / 阿里百炼 |
| API密钥 | 加密存储 |
| 模型选择 | DeepSeek V3 / GPT-4 等 |
| 温度参数 | 控制AI创造性（建议0.3） |
| 超时设置 | API调用超时时间 |
| 重试次数 | 失败后重试次数 |
| 批改提示词 | 可自定义AI批改prompt |

#### 2.9.2 系统参数配置
| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| 登录失败锁定次数 | 5次 | 超过后锁定账户 |
| 锁定时长 | 15分钟 | 解锁等待时间 |
| 默认密码 | 123456 | 新用户初始密码 |
| 单次答题最大时长 | 120分钟 | 超时自动交卷 |
| 文件上传大小限制 | 10MB | 图片、Excel等 |
| 分页每页数量 | 20条 | 列表分页 |

#### 2.9.3 数据备份配置
- 自动备份周期（每日/每周）
- 备份保留数量
- 手动备份功能
- 数据恢复功能

#### 2.9.4 操作日志
- 用户登录日志
- 敏感操作日志（删除、修改等）
- 系统异常日志
- 支持按时间、用户、操作类型筛选

---

## 三、数据库设计

### 3.1 核心数据表

#### 学生表 (students)
```sql
CREATE TABLE students (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  student_no VARCHAR(12) UNIQUE NOT NULL,
  name VARCHAR(20) NOT NULL,
  gender VARCHAR(2),
  grade VARCHAR(10),
  class_name VARCHAR(10),
  parent_name VARCHAR(20),
  phone VARCHAR(15),
  remark TEXT,
  password VARCHAR(64) NOT NULL,
  salt VARCHAR(32),
  status VARCHAR(10) DEFAULT 'active',
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

#### 教师表 (teachers)
```sql
CREATE TABLE teachers (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  teacher_no VARCHAR(10) UNIQUE NOT NULL,
  name VARCHAR(20) NOT NULL,
  gender VARCHAR(2),
  subjects TEXT,  -- JSON数组
  phone VARCHAR(15),
  manage_classes TEXT,  -- JSON数组
  remark TEXT,
  password VARCHAR(64) NOT NULL,
  salt VARCHAR(32),
  status VARCHAR(10) DEFAULT 'active',
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

#### 题目表 (questions)
```sql
CREATE TABLE questions (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  question_type VARCHAR(20) NOT NULL,  -- choice/fill/subjective
  subject VARCHAR(10) NOT NULL,
  grade VARCHAR(10),
  chapter VARCHAR(50),
  content TEXT NOT NULL,
  options TEXT,  -- JSON数组，选择题用
  answer TEXT NOT NULL,
  analysis TEXT,
  difficulty VARCHAR(10) DEFAULT 'medium',
  score DECIMAL(5,2) DEFAULT 2,
  use_count INTEGER DEFAULT 0,
  correct_rate DECIMAL(5,2),
  is_special BOOLEAN DEFAULT 0,  -- 是否专项考题
  status VARCHAR(10) DEFAULT 'active',
  created_by INTEGER,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

#### 考试任务表 (exam_tasks)
```sql
CREATE TABLE exam_tasks (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  title VARCHAR(100) NOT NULL,
  subject VARCHAR(10) NOT NULL,
  grade VARCHAR(10),
  target_classes TEXT,  -- JSON数组
  creator_id INTEGER NOT NULL,
  creator_name VARCHAR(20),
  start_time DATETIME,
  end_time DATETIME,
  duration INTEGER DEFAULT 0,  -- 分钟，0不限时
  questions TEXT,  -- JSON数组，包含题目ID和分值
  total_score DECIMAL(5,2),
  grading_mode VARCHAR(20),  -- ai/manual/mixed
  status VARCHAR(20) DEFAULT 'draft',
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

#### 答题记录表 (exam_answers)
```sql
CREATE TABLE exam_answers (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  task_id INTEGER NOT NULL,
  student_id INTEGER NOT NULL,
  answers TEXT,  -- JSON数组
  start_time DATETIME,
  submit_time DATETIME,
  time_used INTEGER,  -- 秒
  total_score DECIMAL(5,2),
  status VARCHAR(20),  -- ongoing/submitted/graded
  graded_at DATETIME,
  graded_by INTEGER,  -- 教师ID或0表示AI
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

#### 题目作答详情表 (question_answers)
```sql
CREATE TABLE question_answers (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  exam_answer_id INTEGER NOT NULL,
  question_id INTEGER NOT NULL,
  student_answer TEXT,
  score DECIMAL(5,2),
  is_correct BOOLEAN,
  ai_score DECIMAL(5,2),
  ai_comment TEXT,
  teacher_score DECIMAL(5,2),
  teacher_comment TEXT,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

#### 系统配置表 (system_config)
```sql
CREATE TABLE system_config (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  config_key VARCHAR(50) UNIQUE NOT NULL,
  config_value TEXT,
  description TEXT,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

#### 操作日志表 (operation_logs)
```sql
CREATE TABLE operation_logs (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  user_type VARCHAR(10),
  user_id INTEGER,
  user_name VARCHAR(20),
  action VARCHAR(50),
  target_type VARCHAR(20),
  target_id INTEGER,
  detail TEXT,
  ip_address VARCHAR(50),
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

---

## 四、API接口设计

### 4.1 认证接口

#### 登录
```
POST /api/auth/login
Request:
{
  "userType": "student",  // student/teacher/admin
  "account": "202601001",
  "password": "123456"
}
Response:
{
  "code": 0,
  "data": {
    "token": "eyJhbGciOiJIUzI1NiIs...",
    "userInfo": { ... }
  }
}
```

#### 修改密码
```
POST /api/auth/change-password
Request:
{
  "oldPassword": "123456",
  "newPassword": "newpass123"
}
```

### 4.2 学生管理接口

#### 获取学生列表
```
GET /api/students?page=1&grade=一年级&class=1班&keyword=张
Response:
{
  "code": 0,
  "data": {
    "list": [...],
    "total": 100,
    "page": 1,
    "pageSize": 20
  }
}
```

#### 新增学生
```
POST /api/students
Request:
{
  "studentNo": "202601001",
  "name": "张三",
  "gender": "男",
  "grade": "一年级",
  "className": "1班",
  ...
}
```

#### 批量导入学生
```
POST /api/students/import
Request: multipart/form-data (Excel文件)
Response:
{
  "code": 0,
  "data": {
    "success": 48,
    "failed": 2,
    "errors": [
      { "row": 5, "reason": "学号已存在" },
      { "row": 12, "reason": "姓名不能为空" }
    ]
  }
}
```

### 4.3 题库接口

#### 获取题库目录
```
GET /api/questions/tree
Response:
{
  "code": 0,
  "data": [
    {
      "subject": "语文",
      "grades": [
        {
          "grade": "一年级",
          "chapters": [
            { "chapter": "第一章 拼音", "count": 25 },
            ...
          ]
        }
      ]
    }
  ]
}
```

#### 获取题目列表
```
GET /api/questions?subject=语文&grade=一年级&chapter=第一章&type=choice
```

#### 添加题目
```
POST /api/questions
Request:
{
  "questionType": "choice",
  "subject": "语文",
  "grade": "一年级",
  "chapter": "第一章 拼音",
  "content": "下列哪个是韵母？",
  "options": ["a", "b", "c", "d"],
  "answer": "B",
  "analysis": "韵母是指...",
  "difficulty": "easy",
  "score": 2
}
```

### 4.4 考试任务接口

#### 创建考试任务
```
POST /api/exam-tasks
Request:
{
  "title": "一年级语文第一单元测试",
  "subject": "语文",
  "grade": "一年级",
  "targetClasses": ["1班", "2班"],
  "startTime": "2026-04-20 08:00:00",
  "endTime": "2026-04-20 18:00:00",
  "duration": 30,
  "questions": [
    { "questionId": 1, "score": 2 },
    { "questionId": 2, "score": 2 }
  ],
  "gradingMode": "ai"
}
```

#### 自动出题
```
POST /api/exam-tasks/auto-generate
Request:
{
  "subject": "语文",
  "grade": "一年级",
  "chapters": ["第一章", "第二章"],
  "questionConfig": [
    { "type": "choice", "count": 10, "score": 2 },
    { "type": "fill", "count": 5, "score": 2 },
    { "type": "subjective", "count": 2, "score": 10 }
  ],
  "difficultyRatio": { "easy": 0.3, "medium": 0.5, "hard": 0.2 }
}
Response:
{
  "code": 0,
  "data": {
    "questions": [...],
    "totalScore": 50
  }
}
```

#### 获取待答题列表（学生端）
```
GET /api/student/exams
Response:
{
  "code": 0,
  "data": [
    {
      "id": 1,
      "title": "一年级语文第一单元测试",
      "subject": "语文",
      "creatorName": "王老师",
      "endTime": "2026-04-20 18:00:00",
      "questionCount": 17,
      "duration": 30,
      "status": "pending"  // pending/ongoing/submitted/expired
    }
  ]
}
```

#### 提交答题
```
POST /api/student/exams/{taskId}/submit
Request:
{
  "answers": [
    { "questionId": 1, "answer": "B" },
    { "questionId": 2, "answer": "北京" }
  ],
  "timeUsed": 1250  // 秒
}
```

### 4.5 AI批改接口

#### AI批改填空题
```
POST /api/grading/ai-fill
Request:
{
  "questionId": 2,
  "question": "中国的首都是____",
  "referenceAnswer": "北京",
  "studentAnswer": "北京"
}
Response:
{
  "code": 0,
  "data": {
    "isCorrect": true,
    "score": 2,
    "comment": "回答正确"
  }
}
```

#### AI批改主观题
```
POST /api/grading/ai-subjective
Request:
{
  "questionId": 5,
  "question": "请简要介绍你的家乡",
  "referenceAnswer": "要求：提到地理位置、特色、感受",
  "scoringStandard": "地理位置2分，特色2分，感受1分",
  "studentAnswer": "我的家乡在北京...",
  "totalScore": 5
}
Response:
{
  "code": 0,
  "data": {
    "score": 4,
    "comment": "文章结构清晰，提到了地理位置和特色，但个人感受表达不够充分..."
  }
}
```

---

## 五、前端页面结构

### 5.1 学生端页面
```
/student
├── /login              # 登录页
├── /home               # 主页
├── /pending            # 待答题列表
├── /exam/:id           # 答题任务详情
├── /exam/:id/start     # 答题界面
├── /exam/:id/check     # 检查界面
├── /exam/:id/result    # 答题结果
├── /records            # 答题记录
├── /records/:id        # 记录详情
├── /mistakes           # 错题库
├── /profile            # 个人信息
└── /change-password    # 修改密码
```

### 5.2 教师端页面
```
/teacher
├── /login              # 登录页
├── /home               # 主页
├── /students           # 学生管理
│   ├── /list           # 学生列表
│   ├── /add            # 新增学生
│   ├── /edit/:id       # 编辑学生
│   └── /import         # 批量导入
├── /questions          # 题库管理
│   ├── /tree           # 题库目录
│   ├── /list           # 题目列表
│   ├── /add            # 添加题目
│   ├── /edit/:id       # 编辑题目
│   ├── /import         # 批量导入
│   └── /special        # 专项考题
├── /exams              # 出题发布
│   ├── /list           # 任务列表
│   ├── /create         # 新建任务
│   ├── /auto           # 自动出题
│   ├── /manual         # 手动出题
│   └── /edit/:id       # 编辑任务
├── /grading            # 批改管理
│   ├── /pending        # 待批改列表
│   └── /grade/:id      # 批改界面
├── /statistics         # 数据统计
├── /profile            # 个人信息
└── /change-password    # 修改密码
```

### 5.3 管理员端页面
```
/admin
├── /login              # 登录页
├── /home               # 主页（数据概览）
├── /teachers           # 教师管理
│   ├── /list
│   ├── /add
│   ├── /edit/:id
│   └── /import
├── /students           # 学生管理（全校）
├── /questions          # 题库管理（全校）
├── /exams              # 考试任务管理（全校）
├── /settings           # 系统配置
│   ├── /ai             # AI批改配置
│   ├── /system         # 系统参数
│   └── /backup         # 数据备份
├── /logs               # 操作日志
├── /statistics         # 全局统计
└── /profile            # 个人信息
```

---

## 六、特色功能设计

### 6.1 智能出题权重算法
```javascript
// 计算题目权重（权重越高越可能被抽中）
function calculateWeight(question, recentExams) {
  let weight = 100;
  
  // 减少近期出过的题
  const usedInRecent = recentExams.filter(
    exam => exam.questions.includes(question.id)
  ).length;
  weight -= usedInRecent * 20;
  
  // 平衡正确率，优先选中档题
  if (question.correctRate > 0.8) weight -= 10; // 太简单
  if (question.correctRate < 0.3) weight -= 10; // 太难
  
  // 使用次数少的优先
  weight -= question.useCount;
  
  return Math.max(weight, 1);
}
```

### 6.2 AI批改提示词模板
```
你是一位小学教师，正在批改学生的填空题/主观题。

题目：{question}
参考答案：{referenceAnswer}
评分标准：{scoringStandard}
学生答案：{studentAnswer}
满分：{totalScore}

请判断学生答案是否正确，并给出得分和简短评语。
要求：
1. 对于填空题，判断是否与参考答案语义相近
2. 对于主观题，根据评分标准逐项打分
3. 评语要具体、鼓励性

请以JSON格式返回：
{
  "isCorrect": true/false,
  "score": 数字,
  "comment": "评语"
}
```

### 6.3 响应式设计断点
```css
/* 移动端 */
@media (max-width: 767px) { ... }

/* 平板 */
@media (min-width: 768px) and (max-width: 1023px) { ... }

/* 桌面端 */
@media (min-width: 1024px) { ... }
```

### 6.4 答题状态实时保存
- 每30秒自动保存答案到本地存储
- 每答完一题自动保存到服务器
- 断网时本地存储，联网后自动同步
- 异常退出后可恢复答题进度

---

## 七、安全设计

### 7.1 数据安全
- 密码使用bcrypt加密存储
- API密钥加密存储，不暴露给前端
- 敏感数据传输使用HTTPS
- SQL注入防护（使用参数化查询）

### 7.2 权限控制
- JWT Token认证
- 接口权限校验中间件
- 前端路由守卫
- 数据隔离（教师只能操作所管班级）

### 7.3 防作弊设计
- 答题时禁止复制粘贴
- 切换标签页检测并记录
- 限时模式防止超时继续答题
- 答题顺序随机（可选）

---

## 八、开发计划

### Phase 1：基础框架（2周）
- [ ] 项目初始化（前后端）
- [ ] 数据库设计与初始化
- [ ] 用户认证模块
- [ ] 基础UI框架搭建

### Phase 2：核心功能（3周）
- [ ] 学生管理模块
- [ ] 教师管理模块
- [ ] 题库管理模块
- [ ] 出题发布模块

### Phase 3：答题系统（2周）
- [ ] 学生答题界面
- [ ] 答题记录与错题库
- [ ] 批改模块

### Phase 4：AI集成（1周）
- [ ] AI批改接口对接
- [ ] 批改结果优化

### Phase 5：统计与优化（1周）
- [ ] 数据统计模块
- [ ] 系统配置模块
- [ ] 性能优化
- [ ] 测试与修复

### Phase 6：部署上线（1周）
- [ ] 生产环境部署
- [ ] 数据迁移
- [ ] 用户培训

---

## 九、附录

### 9.1 科目与章节参考

#### 语文
| 年级 | 章节 |
|------|------|
| 一年级 | 拼音、识字、词语、句子、阅读理解、看图写话 |
| 二年级 | 识字、词语、句子、阅读理解、看图写话 |
| 三年级 | 词语、句子、阅读理解、作文 |
| 四年级 | 词语、句子、阅读理解、作文 |
| 五年级 | 词语、句子、阅读理解、作文 |
| 六年级 | 词语、句子、阅读理解、作文 |

#### 数学
| 年级 | 章节 |
|------|------|
| 一年级 | 认识数字、加减法、图形、位置、时间 |
| 二年级 | 乘除法、长度单位、角、统计 |
| 三年级 | 四则运算、分数初步、面积、时间计算 |
| 四年级 | 大数认识、小数、平行四边形、统计 |
| 五年级 | 小数四则运算、因数倍数、体积、简易方程 |
| 六年级 | 分数四则运算、百分数、圆、比例、统计 |

#### 英语
| 年级 | 章节 |
|------|------|
| 三年级 | 字母、问候语、数字、颜色、家庭 |
| 四年级 | 学校、食物、动物、身体部位、天气 |
| 五年级 | 日常活动、节日、职业、交通、健康 |
| 六年级 | 过去时、将来时、阅读理解、写作 |

#### 科学
| 年级 | 章节 |
|------|------|
| 三年级 | 植物、动物、天气、材料 |
| 四年级 | 声音、光、人体、地球 |
| 五年级 | 生物与环境、运动和力、热、环境 |
| 六年级 | 微生物、能量、宇宙、科学探究 |

### 9.2 错误码定义
| 错误码 | 说明 |
|--------|------|
| 0 | 成功 |
| 1001 | 用户名或密码错误 |
| 1002 | 账户已被锁定 |
| 1003 | Token失效 |
| 2001 | 学号已存在 |
| 2002 | 学生不存在 |
| 3001 | 题目不存在 |
| 3002 | 题库为空 |
| 4001 | 考试任务不存在 |
| 4002 | 考试未开始 |
| 4003 | 考试已结束 |
| 4004 | 已提交过 |
| 5001 | AI批改失败 |
| 5002 | API配置错误 |

---

*文档结束*
